steps for git commit instruction
1. save local changes
2. staging (git add .(everything))
3. commit (git commit -m 'commit message')
4.sync(push/pull) [git push -u origin]

Important terminal commands
clear

how to get git remote information?
git remote -v

how to get git status?
git status

what is python?
python is a high level object oriented programing language.